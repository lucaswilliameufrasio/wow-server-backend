meta {
  name: Refresh Token
  type: http
  seq: 3
}

post {
  url: {{base_url}}/v1/auth/refresh
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
