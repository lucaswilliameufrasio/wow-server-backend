meta {
  name: Sign In
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/auth/sign-in
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "{{username}}",
    "password": "{{password}}"
  }
}
