meta {
  name: Admin Lock/Unlock Player
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/v1/admin/players/{{account_id}}/lock
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "locked": true
  }
}
