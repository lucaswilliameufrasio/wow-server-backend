apiVersion: v1
kind: Service
metadata:
  name: wow-backend
  namespace: wow-backend
spec:
  selector:
    app: wow-backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
